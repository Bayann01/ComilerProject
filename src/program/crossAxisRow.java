package program;

public class crossAxisRow extends rowProperty{
    String Axis ;

    public crossAxisRow(String axis) {
        Axis = axis;
    }

    @Override
    public String toString() {
        return "crossAxisRow{" +
                "Axis='" + Axis + '\'' +
                '}';
    }
}

